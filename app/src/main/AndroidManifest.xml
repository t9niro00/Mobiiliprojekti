<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.komponenttikirjasto" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KomponenttiKirjasto" >
        <activity android:name="com.example.elecstore.tuoteGet2" >
        </activity>
        <activity android:name=".settings" />
        <activity
            android:name=".komponentit_selaus"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".mikrokontrollerit_selaus"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.elecstore.getData" />
        <activity android:name="com.example.elecstore.tuoteGet" />
        <activity android:name="com.example.elecstore.getKompoData" />
        <activity android:name=".ostoskori"/>
    </application>

</manifest>